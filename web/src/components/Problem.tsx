/** S2 — THE BORDER PROBLEM. Two-tone statement, wraith-style. */
export function Problem() {
  return (
    <section className="problem">
      <div>
        <p className="tag">
          01 · THE BORDER <i>[ ONE CHAIN · ONE TRUTH ]</i>
        </p>
        <h2 className="will-reveal">
          agents can prove they behaved — <em>inside one chain.</em>
          <span className="dim">
            {" "}
            the moment value moves to another L1, <em>trust used to die at the border.</em>
          </span>
        </h2>
      </div>
      <div className="aside will-reveal">
        walled ecosystems answer this
        <br />
        by locking identity to their own chain.
        <br />
        <b>verglas lets the proof travel instead</b> —<br />
        open standard, ERC-8004,
        <br />
        native Avalanche messaging.
      </div>
    </section>
  );
}
